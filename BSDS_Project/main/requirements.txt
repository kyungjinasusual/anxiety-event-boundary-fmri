# LLaVA-NeXT Video Captioning Requirements
# Install LLaVA-NeXT first:
#   pip install git+https://github.com/LLaVA-VL/LLaVA-NeXT.git

# Core dependencies
torch>=2.1.0
transformers>=4.37.0
accelerate>=0.25.0

# Video processing
decord>=0.6.0
av>=10.0.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# Flash Attention (optional but recommended for efficiency)
# Install separately with: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# For 72B model (optional, requires more VRAM)
# bitsandbytes>=0.41.0  # For 8-bit quantization
