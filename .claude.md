# Anxiety Event Boundary 프로젝트 지침

## 🔴 필수 규칙 (매 세션 적용)

### Git Push 자동화
**모든 의미 있는 작업 완료 후 반드시 실행:**
```bash
git add .
git commit -m "적절한 커밋 메시지"
git push origin main
```

### 작업 완료 체크리스트
- [ ] 코드 변경 시 → git commit & push
- [ ] 분석 완료 시 → 결과 저장 & push
- [ ] 문서 작성 시 → git commit & push
- [ ] 세션 종료 전 → **반드시 git push 확인!**

---

## 📋 프로젝트 개요

### 연구 주제
불안장애에서 사건 분절(Event Segmentation)의 신경 메커니즘

### 핵심 질문
1. 불안이 사건 경계(event boundary) 탐지에 어떤 영향을 미치는가?
2. 해마(hippocampus)의 패턴 유사성이 불안 수준에 따라 달라지는가?
3. HMM/BSDS 기반 상태 전환이 불안군에서 어떻게 다른가?

### 핵심 키워드
- event segmentation, event boundary, anxiety
- fMRI, hippocampus, pattern similarity
- HMM (Hidden Markov Model), BSDS
- neural state transitions

---

## 📁 프로젝트 구조

```
anxiety-event-boundary/
├── analysis/           # 분석 스크립트
├── data/               # 데이터 (큰 파일은 .gitignore)
├── results/            # 분석 결과
├── figures/            # 그림
├── docs/               # 문서
└── BSDS_Project/       # BSDS 관련 코드 (서브프로젝트)
```

---

## 🛠️ 기술 스택

### 분석 도구
- Python: nibabel, nilearn, MNE
- R: brms (베이지안 분석)
- MATLAB: SPM12

### 주요 라이브러리
```python
# fMRI 분석
nibabel, nilearn, nipype

# 통계
scipy, statsmodels, pingouin

# HMM/상태 분석
hmmlearn, pomegranate
```

---

## ⚠️ 주의사항

### 데이터 보안
- 원본 fMRI 데이터는 절대 GitHub에 푸시하지 않음
- .gitignore에 *.nii, *.nii.gz 포함 확인

### 분석 재현성
- 랜덤 시드 항상 고정: `np.random.seed(42)`
- 분석 파라미터는 config 파일로 관리

### 백업 위치
- 이전 버전들: `~/github/_backups/anxiety-event-boundary/`
